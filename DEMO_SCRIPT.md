# 数字庞贝 · 3-5 分钟路演脚本

> 纯文字演讲稿，约 4 分钟。括号内为停顿/语气提示，不需要朗读。
> 赛道：Z.AI · Web3 × Long-Horizon Task。

---

大家好，我们的项目叫 **Digital Pompeii，数字庞贝**。

公元 79 年，维苏威火山爆发，庞贝城被火山灰封存。两千年后，人们走进那座废墟，不只是看见一场灾难，也看见一个时代留下来的证据。

今天，区块链其实也在做类似的事。一个 Web3 项目可以被黑、跑路、崩盘，团队可以消失，官网可以下线，但它留下的智能合约、交易、事件日志和资金流，会一直留在链上。

这些失败不是没有证据，而是普通用户很难读懂。

所以我们做了数字庞贝：**一座链上灾难博物馆，也是一名 AI 验尸官**。它把复杂的链上事故，变成可读、可复核、可被后来者学习的死亡报告。

---

为什么要做这个？

Web3 的失败成本非常高，但失败经验经常没有被很好地保存下来。很多用户只知道“某项目被黑了”“某协议损失了多少钱”，却不知道攻击是怎么发生的、风险信号在哪里、自己下次应该避免什么。

数字庞贝想解决的，就是这个断层。

我们把链上灾难做成博物馆展品。现在已经收录了五个真实案例，跨越 2016 到 2022，包括 The DAO、Parity 多签钱包、Ronin、Beanstalk 和 Nomad，合计损失 **13.57 亿美元**。每一件展品都不是故事改写，而是一份可以回到 Etherscan 逐条复核的事故档案。

这也是它的公共教育意义：我们不是制造恐惧，而是把 Web3 中昂贵的失败转化为公共知识。让新用户、开发者和项目方，都能从过去的事故里学到具体的安全教训。

---

核心亮点有两个。

第一个是 **AI 验尸官**。

用户输入一个合约地址之后，GLM-5.1 驱动的 Agent 会自主进行多轮调查。它不是一次性问答，也不是把人工资料交给模型润色。

它会自己决定下一步该查什么：先读取合约源码，判断代理合约、库合约或钻石结构；再查看事件日志，寻找异常转账、权限变更和可疑调用；再追踪资金流，定位关键交易；提出死因假设后，还会主动寻找反证。如果证据冲突，它会修正结论，而不是强行编故事。

一个案例通常需要 6 到 12 轮工具调用。模型必须在长程任务里保持目标一致，不能提前结案，也不能用幻觉填补链上空白。这正是我们使用 GLM-5.1 的核心场景。

---

第二个亮点是 **灾难博物馆式策展**。

我们不是只输出一份冷冰冰的审计报告。每个案例都会生成双层展品。

第一层是技术尸检：死因、置信度、关键交易、时间线、损失规模和证据链。每条技术结论都带链上证据，可以人工复核。

第二层是策展表达：用更容易理解的语言，把事故变成一件能被普通人读懂的展品。比如 The DAO 的核心教训，不只是“发生了重入攻击”，而是“先改状态，再转账”这个工程原则为什么重要。

我们的原则是：**先调查，后文学；技术层零虚构**。文学表达只服务于已查实的事实，不能替代证据。

---

技术上，我们最想展示的是对“翻新后的废墟”的处理。

Beanstalk 和 Nomad 这类协议在攻击之后都经历过升级或迁移。如果 Agent 只读取 latest 状态，看到的是修复后的版本，不是案发现场。

所以我们做了 `get_upgrade_history`，让 Agent 可以回放代理合约和实现合约的升级历史，定位攻击发生时真正运行的代码版本。

Beanstalk 的案例里，Agent 不需要预先知道攻击日期。它通过两个链上信号锁定关键位置：一个未经验证的可疑 init 合约，以及那笔交易中异常高的 121 条事件日志。相比日常治理操作最多 8 条日志，这个异常非常明显。Agent 最终定位到对应的 DiamondCut，并完成纯 AI 模式结案。

这说明它不是在复述资料，而是在用工具和链上证据做自主调查。

---

当前版本是一个可演示原型：链上灾难博物馆 + 免费合约体检。

博物馆负责沉淀真实事故案例；“入坑前，先体检”让用户粘贴合约地址，快速获得自然语言风险提示。我们希望它先成为一个免费的 Web3 安全入口。

下一步，我们会把事故复盘能力前置，做成 **防猝死指数 Anti-Sudden-Death Index**。它不替代专业审计，而是提供更早期、更高频、更容易理解的风险信号层，服务项目方自查、VC 尽调、Launchpad 初筛、Grant 和黑客松项目筛选。

长期来看，数字庞贝的护城河是 **Failure Archive，死因库**。每新增一个真实事故，系统就多沉淀一种失败模式：攻击路径、权限配置、资金流异常、治理漏洞、用户误操作和项目崩盘信号。这个数据资产会反过来让体检和评分更准确。

---

最后，我想用一句话总结。

Web3 不缺链上数据，缺的是能让人真正理解风险的解释层。

数字庞贝想做的，就是把链上的失败保存下来、解释清楚，并变成后来者可以使用的公共知识。

我们不能让已经死去的项目复活，但我们可以让它们的死亡变得有意义。

这就是 **Digital Pompeii，数字庞贝**。

---

## 备用速答（Q&A）

- **「这跟直接问 GPT 有什么区别？」**：它不是一次性问答，而是**自主多轮工具调用 + 自我纠错**；全过程有 `runs/` 下可复现的审计日志，每条结论都钉在真实链上证据上。
- **「Web3 体现在哪？」**：① 只读真实链上数据（Etherscan）还原死因；② 展品哈希可经 OpenTimestamps 锚定为不可篡改凭证（可选模块）；③ 主题即「链上不可篡改性」本身。
- **「数据准不准？」**：5 案全部纯 AI 模式自主定因，死因与外部权威复盘一致，核查留档于 `FACT_CHECK.md`。
- **「最难的技术点？」**：可升级合约「翻新后的废墟」——用 `get_upgrade_history` 做升级考古，靠链上足迹（未验证 init + 最高交易日志数）**日期无关地**自动锁定攻击实现，不依赖任何外部已知信息。
