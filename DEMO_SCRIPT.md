# 数字庞贝 · 路演发言稿

> 纯文字演讲稿，约 4 分钟。括号内为停顿/语气提示，不需要朗读。
> 赛道：Z.AI · Web3 × Long-Horizon Task。

---

公元 79 年，维苏威火山爆发，把庞贝城里的每一个人——每一个逃跑的姿势、每一顿没吃完的饭——瞬间凝固在火山灰里。两千年后，考古学家走进去，像走进一个没有血迹的案发现场。

今天，区块链也在做同一件事。只不过它凝固的，不是古城，是**死去的加密协议**。

一个项目可以崩盘，可以被黑客掏空，可以就此消失。但它的尸体——智能合约、每一笔交易、每一行事件日志——会永远、不可篡改地留在链上。

我们做的，就是一座为这些尸体而建的博物馆。我们叫它，**Digital Pompeii，数字庞贝**。

---

在 Web2 的世界，失败可以被公关、被删帖、被时间掩埋。但链上的失败删不掉。我们把这一点，变成一种**公共物品**：为每一场已经发生的灾难，立一座不可篡改的警示碑。

这里现在收录了五件展品，跨越 2016 到 2022，从以太坊最早的重入攻击，到史上最大的跨链桥劫案——合计**十三亿五千七百万美元**的链上死亡。每一件，都是一份可追溯、可在链上逐条复核的事故档案。

---

但数字庞贝不是一个"把资料喂给模型润色"的内容工具。它的核心，是一个**自主的链上法医 Agent**，由 GLM-5.1 驱动。

你只需要给它一个合约地址。

它接下来会自主决定每一步的动作：先读源码，判断合约是不是代理、库合约还是钻石结构；再查事件日志，找异常的转账和权限变更；再追踪资金，看 ETH 和代币在攻击那一刻去了哪里；提出一个死因假设之后，它会**主动去找反证**——如果证据冲突，就推翻重来。

一个案子，它自主走了六到十二轮工具调用，**每一步查什么、都是它自己决定的**。这正是"长程任务"的核心挑战：模型要在十几步里始终不跑偏，不提前结案，不用幻觉填补链上空白。

---

然后我们遇到了最难的两个案子：Beanstalk 和 Nomad。

这两个协议被攻击之后，都经历了升级或迁移——Agent 直接去查，看到的是"翻新后的废墟"，不是案发现场。

于是我们给它造了一把**考古铲**：一个新工具，可以回放合约的完整升级历史，把攻击当天真正在岗的那个版本的代码，从链上历史里挖出来。

Beanstalk 的破案过程是我们最自豪的地方。Agent 甚至不需要知道攻击哪天发生。它只看到两个链上信号：一个**未经验证的可疑 init 合约**，加上那笔交易里**异常高的 121 条事件日志**——做个参照，日常的治理操作最多产生 8 条。闪贷掏空协议，引爆了 121 条 Transfer 事件。Agent 靠这两个足迹，**自己锁定了凶手，精确到那一次 DiamondCut**。

靠这把铲子，这两个案子从半人工辅助，升级成了**纯 AI 自主结案**。

---

每件展品都是双层的。

上层是冷峻的技术尸检：死因、置信度、每一条带交易哈希的证据链，全部可以打开 Etherscan 自行复核。

下层是文学策展：一段墓志铭，一句给后来的建造者留下的警示。比如 The DAO 的那句——"先关门，再转账。Checks, Effects, Interactions，这个顺序不是建议，是图腾。"

我们的铁律是：**先调查，后文学；技术层零虚构**。墓志铭里的每一个数字，必须在链上有据可查。五件展品的每一处数据和死因，我们都逐条对照过外部权威资料——核查记录留在仓库的 FACT_CHECK.md 里，这不是自说自话。

---

而这只是开始。同一套法医能力，可以**调转枪口做预防**：上线前死因体检——拿一个新项目，按历史死因库逐条比对它的猝死风险。我们每验一具尸，死因库就厚一层，体检就更准。这是一个会**自我复利、抄不走**的数据资产。

（停顿）

链上的规则可以被完美执行，却无法阻止人去背叛规则。代码可以不可篡改，却挡不住一行被设成零的初始化参数。

我们没法让这些项目复活。但我们可以让它们的死，**有人记得，有人读懂，有人因此活下来**。

这就是数字庞贝——**一座删不掉的警示碑**。

---

## 备用速答（Q&A）

- **「这跟直接问 GPT 有什么区别？」**：它不是一次性问答，而是**自主多轮工具调用 + 自我纠错**；全过程有 `runs/` 下可复现的审计日志，每条结论都钉在真实链上证据上。
- **「Web3 体现在哪？」**：① 只读真实链上数据（Etherscan）还原死因；② 展品哈希可经 OpenTimestamps 锚定为不可篡改凭证（可选模块）；③ 主题即「链上不可篡改性」本身。
- **「数据准不准？」**：5 案全部纯 AI 模式自主定因，死因与外部权威复盘一致，核查留档于 `FACT_CHECK.md`。
- **「最难的技术点？」**：可升级合约「翻新后的废墟」——用 `get_upgrade_history` 做升级考古，靠链上足迹（未验证 init + 最高交易日志数）**日期无关地**自动锁定攻击实现，不依赖任何外部已知信息。
